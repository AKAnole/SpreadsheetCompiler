package lexer;

public enum TokenType {

    NUMBER,
    CELL_REFERENCE,
    IDENTIFIER,
    EQUALS,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    LPAREN,
    RPAREN,
    COMMA,
    COLON,
    EOF,
    ILLEGAL
}
